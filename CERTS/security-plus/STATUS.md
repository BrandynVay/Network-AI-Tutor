# Security+ — not started

This track begins after the Network+ track is passed (see the project
root `CLAUDE.md` § Certification tracks for the order and the process).

**Target exam (best current knowledge, verify when this track starts —
CompTIA revises Security+ on a multi-year cycle):** SY0-701.

Nothing else exists here yet on purpose — this project builds each
track's content just-in-time, not speculatively. When this track starts:

1. Run the same pipeline Network+ used: interview → diagnostic quiz →
   assessment → `OUTLINE.md` → `STUDENT_PROFILE.md` → Day 01.
2. Carry forward anything genuinely reusable from the Network+ track's
   `STUDENT_PROFILE.md` (e.g., preferred learning mode, time budget,
   general study patterns) so the student doesn't re-answer settled
   questions — but re-diagnose domain-level knowledge fresh; passing
   Network+ doesn't establish Security+ readiness.
3. Delete this file once real content replaces it.
